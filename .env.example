# Environment Configuration
NODE_ENV=development

# Claude Code SDK Configuration (Required)
# For subscription users: Session-based authentication (preferred)
# For API key users: Set your Anthropic API key
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# External Service URLs
REDIS_URL=redis://localhost:6400
QDRANT_URL=http://localhost:6350

# Service Configuration
QDRANT_HOST=localhost
QDRANT_PORT=6350
REDIS_HOST=localhost
REDIS_PORT=6400
REDIS_DB=0

# Logging Level
LOG_LEVEL=info

# Note: Most configuration is now managed through specmgr.config.yaml
# This includes server settings, document paths, search parameters, etc.
# Only environment-specific and secret values should be set here.
